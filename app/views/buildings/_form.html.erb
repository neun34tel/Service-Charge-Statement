<%= form_with(model: building) do |form| %>
  <% if building.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
        <% building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :total_residential_area, style: "display: block" %>
    <%= form.text_field :total_residential_area %>
  </div>

  <div>
    <%= form.label :total_usable_area, style: "display: block" %>
    <%= form.text_field :total_usable_area %>
  </div>

  <div>
    <%= form.label :plot_area, style: "display: block" %>
    <%= form.text_field :plot_area %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
